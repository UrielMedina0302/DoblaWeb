// src/environments/environment.ts
export const environment = {
  production: false,
  apiUrl: 'https://doblaweb-1.onrender.com', // URL de tu backend local
  jwtKey: 'token', // Nombre del token en localStorage (si no usas cookies)
};